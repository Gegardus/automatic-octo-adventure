<div class="PageHeader print:mb-1 flex flex-wrap justify-between items-center">
  <h1 class="mt-6 mb-6 text-xl sm:text-2xl font-extrabold text-gray-900 flex-shrink flex-grow min-w-0 <%= header_classes %>"><%= heading %></h1>
  <%= yield %>
</div>
